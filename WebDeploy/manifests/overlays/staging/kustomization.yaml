apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -staging

images:
  - name: dominicnygard/pharm-sim
    newTag: staging

patches:
  - path: deployment-patch.yaml
  - path: route-patch.yaml
